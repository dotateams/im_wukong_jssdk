# 实施任务

## SDK

- [ ] 为 `DeviceDirectory` 增加长期群设备目录缓存测试。
- [ ] 为浏览器本地持久化缓存增加测试，验证刷新页面后仍可复用设备目录。
- [ ] 为 stale-while-revalidate 行为增加测试：有缓存立即返回，后台刷新成功后替换缓存。
- [ ] 为同频道并发刷新去重增加测试。
- [ ] 为版本不一致后的强制刷新增加测试。
- [ ] 将 `channelDevicesCacheTtlMs` 改为默认不过期或仅作为可选兜底配置。
- [ ] 实现按账号、设备、频道隔离的持久化缓存读写。
- [ ] 实现有缓存立即返回、后台刷新、失败保留旧缓存。
- [ ] 接入服务端群 E2EE 设备目录版本接口。
- [ ] 确认 `invalidateGroupMemberCache(channel)` 能清理长期缓存和 in-flight 状态。
- [ ] 跑通 SDK E2EE 测试。

## 验证

- [ ] 压测脚本增加“重复切换同一加密群”的设备目录请求次数统计。
- [ ] 压测脚本增加“群成员变动后缓存失效并重新获取”的验证。
- [ ] Chrome 实测：长时间不进入加密群后再次进入，不应阻塞首屏。

## 实施任务

- [ ] 确认分片大小、并发数、会话 TTL、浏览器下载降级策略。
- [ ] 在 SDK 定义 `e2eeMedia.version = 2` manifest 数据结构。
- [ ] 在 SDK 实现分片 key/nonce 派生、AES-GCM 分片加密、密文 SHA-256 校验。
- [ ] 在 SDK 实现上传会话创建、状态查询、分片上传、失败重试、断点续传。
- [ ] 在 SDK 实现分片下载、校验、解密和流式写出适配层。
- [ ] 在 Web 接入上传进度、继续上传、失败重试、下载进度和错误提示。
- [ ] 在 Server 新增分片上传会话、分片状态、分片上传、完成、取消和清理接口。
- [ ] 添加权限校验：只有发送者本人可继续上传自己的会话；只有频道成员可下载密文分片。
- [ ] 增加测试：单聊、群聊、刷新恢复、断网重试、重复上传、缺失分片、hash mismatch、旧 version 兼容。
- [ ] 增加压测脚本：1GB 文件、并发上传、并发下载、失败恢复、MinIO/API 内存观测。
- [ ] Chrome 真实账号测试：上传中刷新、断网恢复、下载大文件、旧小文件兼容。


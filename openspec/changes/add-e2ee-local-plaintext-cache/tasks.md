# 实施任务

> 当前阶段只确认需求文档，不开始编码。

## SDK 任务

- [ ] 设计 E2EE plaintext cache 存储接口，按账号、设备、频道、消息隔离。
- [ ] 将文本消息解密成功后的 `MessageContent` 写入长期明文缓存。
- [ ] 历史消息和实时消息解密前优先读取明文缓存。
- [ ] 调整 sender key 持久化策略，取消时间过期，缺失或失效时再查询服务端 envelope。
- [ ] 为 sender key 恢复请求增加并发去重和短期 403/404 负缓存。
- [ ] 增加媒体缩略图本地缓存，刷新后优先复用。
- [ ] 保持普通大文件点击后懒解密，不自动长期缓存原文。
- [ ] 暴露清理当前账号当前设备 E2EE 本地数据的 SDK API。
- [ ] 增加单元测试覆盖文本明文缓存、sender key 长期持久化、缺 key 恢复、缩略图缓存、大文件懒解密和清理逻辑。

